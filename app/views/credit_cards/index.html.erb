<h1 id="credit_card_title">Credit Cards</h1>

<table id="credit_cards">
  <tr>
    <th>Logo</th>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @credit_cards.each do |credit_card| %>
  <tr>
    <td><%= image_tag(credit_card.logo)  %></td>
    <td><%= credit_card.company %></td>
    <td><%= link_to 'Show', credit_card %></td>
    <td><%= link_to 'Edit', edit_credit_card_path(credit_card) %></td>
    <td><%= link_to 'Destroy', credit_card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Credit card', new_credit_card_path %>
 &nbsp;&nbsp;
 <%= link_to 'Index', home_index_path %>