<%= form_for(@bus_company) do |f| %>
  <% if @bus_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bus_company.errors.count, "error") %> prohibited this bus_company from being saved:</h2>

      <ul>
      <% @bus_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company %><br />
    <%= f.text_field :company %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
