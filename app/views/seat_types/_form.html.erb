<%= form_for(@seat_type) do |f| %>
  <% if @seat_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seat_type.errors.count, "error") %> prohibited this seat_type from being saved:</h2>

      <ul>
      <% @seat_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kind,'Type' %><br />
    <%= f.text_field :kind %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
